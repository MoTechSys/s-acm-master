# 🔐 نظام الصلاحيات الديناميكي

> آخر تحديث: 2026-01-30

---

## 1. المفهوم الأساسي

### الفلسفة

نظام صلاحيات **ديناميكي بالكامل** حيث:
- لا توجد أدوار ثابتة مسبقاً
- الأدمن ينشئ الأدوار بنفسه
- كل عملية في النظام = صلاحية
- الصلاحيات متداخلة (3 مستويات)

### المبادئ

| المبدأ | الشرح |
|--------|-------|
| **ديناميكية كاملة** | الأدمن يحدد الأدوار والصلاحيات |
| **لوحة موحدة** | نفس الواجهة للجميع، تتغير حسب الصلاحيات |
| **تداخل الصلاحيات** | 3 مستويات: قسم → رئيسية → فرعية |
| **إخفاء ذكي** | العناصر غير المسموحة تختفي تلقائياً |

---

## 2. هيكل الصلاحيات

### المستوى الأول: الأقسام (Categories)

```
📁 إدارة المستخدمين
📁 إدارة الأدوار
📁 إدارة المقررات
📁 إدارة الملفات
📁 البيانات الأكاديمية
📁 الإشعارات
📁 الذكاء الاصطناعي
📁 التقارير
📁 الإعدادات
📁 سجلات التدقيق
📁 سلة المحذوفات
📁 لوحة التحكم
```

### المستوى الثاني: الصلاحيات الرئيسية

صلاحيات أساسية داخل كل قسم (مثل: عرض، إضافة، تعديل، حذف)

### المستوى الثالث: الصلاحيات الفرعية

صلاحيات تفصيلية تابعة للصلاحيات الرئيسية

---

## 3. جرد الصلاحيات الكامل

### 📁 إدارة المستخدمين (users)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_users` | عرض المستخدمين | عرض قائمة المستخدمين | `view_user_details`, `export_users` |
| `add_user` | إضافة مستخدم | إضافة مستخدم جديد | - |
| `edit_user` | تعديل مستخدم | تعديل بيانات مستخدم | `change_user_role`, `reset_user_password` |
| `delete_user` | حذف مستخدم | حذف مستخدم | - |
| `activate_user` | تفعيل/تعطيل | تفعيل أو تعطيل مستخدم | - |

---

### 📁 إدارة الأدوار (roles)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_roles` | عرض الأدوار | عرض قائمة الأدوار | `view_role_permissions` |
| `add_role` | إضافة دور | إنشاء دور جديد | - |
| `edit_role` | تعديل دور | تعديل دور وصلاحياته | `edit_role_permissions` |
| `delete_role` | حذف دور | حذف دور | - |

---

### 📁 إدارة المقررات (courses)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_courses` | عرض المقررات | عرض قائمة المقررات | `view_course_details`, `view_course_files` |
| `add_course` | إضافة مقرر | إضافة مقرر جديد | - |
| `edit_course` | تعديل مقرر | تعديل بيانات مقرر | - |
| `delete_course` | حذف مقرر | حذف مقرر | - |
| `assign_instructor` | تعيين مدرس | تعيين مدرس للمقرر | - |

---

### 📁 إدارة الملفات (files)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_files` | عرض الملفات | عرض قائمة الملفات | `download_files`, `preview_files` |
| `upload_files` | رفع ملفات | رفع ملفات جديدة | - |
| `edit_files` | تعديل ملفات | تعديل بيانات الملفات | `rename_files`, `move_files` |
| `delete_files` | حذف ملفات | حذف ملفات | - |
| `approve_files` | الموافقة | الموافقة على الملفات المعلقة | - |

---

### 📁 البيانات الأكاديمية (academic)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_academic` | عرض البيانات | عرض البيانات الأكاديمية | `view_colleges`, `view_departments`, `view_levels` |
| `manage_colleges` | إدارة الكليات | إضافة/تعديل/حذف الكليات | `add_college`, `edit_college`, `delete_college` |
| `manage_departments` | إدارة الأقسام | إضافة/تعديل/حذف الأقسام | `add_department`, `edit_department`, `delete_department` |
| `manage_levels` | إدارة المستويات | إضافة/تعديل/حذف المستويات | `add_level`, `edit_level`, `delete_level` |
| `manage_semesters` | إدارة الفصول | إضافة/تعديل/حذف الفصول | `add_semester`, `edit_semester`, `delete_semester` |

---

### 📁 الإشعارات (notifications)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_notifications` | عرض الإشعارات | عرض الإشعارات الواردة | - |
| `send_notifications` | إرسال إشعارات | إرسال إشعارات للمستخدمين | `send_to_all`, `send_to_role`, `send_to_user` |
| `manage_notifications` | إدارة الإشعارات | حذف الإشعارات | `delete_notification` |

---

### 📁 الذكاء الاصطناعي (ai)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `use_ai_summary` | التلخيص | استخدام ميزة التلخيص | - |
| `use_ai_questions` | توليد الأسئلة | توليد أسئلة من المحتوى | - |
| `use_ai_chat` | اسأل المستند | محادثة مع المستند | - |

---

### 📁 التقارير (reports)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_reports` | عرض التقارير | عرض التقارير | `view_user_reports`, `view_file_reports`, `view_course_reports` |
| `export_reports` | تصدير التقارير | تصدير التقارير | `export_pdf`, `export_excel` |
| `generate_reports` | إنشاء تقارير | إنشاء تقارير مخصصة | - |

---

### 📁 الإعدادات (settings)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_settings` | عرض الإعدادات | عرض صفحة الإعدادات | - |
| `edit_settings` | تعديل الإعدادات | تعديل الإعدادات | `edit_general_settings`, `edit_notification_settings`, `edit_security_settings` |

---

### 📁 سجلات التدقيق (audit)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_audit_logs` | عرض السجلات | عرض سجلات التدقيق | `filter_logs`, `search_logs` |
| `export_audit_logs` | تصدير السجلات | تصدير سجلات التدقيق | - |

---

### 📁 سلة المحذوفات (trash)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_trash` | عرض المحذوفات | عرض العناصر المحذوفة | - |
| `restore_items` | استعادة العناصر | استعادة العناصر المحذوفة | - |
| `permanent_delete` | حذف نهائي | حذف العناصر نهائياً | - |

---

### 📁 لوحة التحكم (dashboard)

| ID | الصلاحية | الوصف | الفرعيات |
|----|----------|-------|----------|
| `view_dashboard` | عرض لوحة التحكم | الوصول للوحة التحكم | - |
| `view_statistics` | عرض الإحصائيات | عرض الإحصائيات | `view_user_stats`, `view_file_stats`, `view_course_stats`, `view_activity_stats` |

---

## 4. قواعد التبعية

### القاعدة الأساسية

```
صلاحية العرض (view) ← أساس لكل العمليات الأخرى
```

### جدول التبعيات

| الصلاحية | تتطلب |
|----------|-------|
| `edit_user` | `view_users` |
| `delete_user` | `view_users` |
| `add_user` | `view_users` |
| `download_files` | `view_files` |
| `edit_files` | `view_files` |
| `delete_files` | `view_files` |
| `export_reports` | `view_reports` |
| `edit_role_permissions` | `edit_role` |
| `view_user_details` | `view_users` |

### سلوك التحديد التلقائي

| الإجراء | النتيجة |
|---------|---------|
| تحديد صلاحية فرعية | تفعيل الصلاحية الرئيسية تلقائياً |
| إلغاء صلاحية رئيسية | إلغاء كل الصلاحيات الفرعية |
| تحديد "تحديد الكل" للقسم | تفعيل كل صلاحيات القسم |

---

## 5. واجهة اختيار الصلاحيات

### التصميم المطلوب

```
┌─────────────────────────────────────────────────────────────┐
│ 📁 إدارة المستخدمين          [☑️ تحديد الكل] [▼ طي/فتح]   │
├─────────────────────────────────────────────────────────────┤
│   ☑️ عرض المستخدمين                                        │
│      ├── ☐ عرض تفاصيل المستخدم                             │
│      └── ☐ تصدير المستخدمين                                │
│   ☐ إضافة مستخدم                                           │
│   ☐ تعديل مستخدم                                           │
│      ├── ☐ تغيير دور المستخدم                              │
│      └── ☐ إعادة تعيين كلمة المرور                         │
│   ☐ حذف مستخدم                                             │
│   ☐ تفعيل/تعطيل مستخدم                                     │
└─────────────────────────────────────────────────────────────┘
```

### الميزات

| الميزة | الوصف |
|--------|-------|
| **Accordion** | طي/فتح كل قسم |
| **Checkbox متداخلة** | 3 مستويات من الاختيارات |
| **تحديد الكل** | زر لكل قسم |
| **التحديد التلقائي** | الأب يفعّل الأبناء |
| **البحث** | البحث في الصلاحيات |

---

## 6. إخفاء العناصر

### القائمة الجانبية

| العنصر | الصلاحية المطلوبة |
|--------|-------------------|
| لوحة التحكم | `view_dashboard` |
| المستخدمين | `view_users` |
| الأدوار | `view_roles` |
| المقررات | `view_courses` |
| الملفات | `view_files` |
| البيانات الأكاديمية | `view_academic` |
| الإشعارات | `view_notifications` |
| الذكاء الاصطناعي | `use_ai_summary` OR `use_ai_questions` OR `use_ai_chat` |
| التقارير | `view_reports` |
| الإعدادات | `view_settings` |
| سجلات التدقيق | `view_audit_logs` |
| سلة المحذوفات | `view_trash` |

### الإحصائيات

| الإحصائية | الصلاحية المطلوبة |
|-----------|-------------------|
| إجمالي المستخدمين | `view_user_stats` |
| إجمالي الملفات | `view_file_stats` |
| إجمالي المقررات | `view_course_stats` |
| النشاط الأخير | `view_activity_stats` |

### الأزرار

| الزر | الصلاحية المطلوبة |
|------|-------------------|
| إضافة مستخدم | `add_user` |
| تعديل مستخدم | `edit_user` |
| حذف مستخدم | `delete_user` |
| رفع ملف | `upload_files` |
| حذف ملف | `delete_files` |
| إضافة دور | `add_role` |
| تعديل دور | `edit_role` |

---

## 7. هيكل البيانات

### Permission

```typescript
interface Permission {
  id: string;           // معرف فريد (مثل: view_users)
  name: string;         // الاسم بالإنجليزية
  nameAr: string;       // الاسم بالعربية
  description: string;  // الوصف
  category: string;     // القسم التابع له
  parent?: string;      // الصلاحية الأب (إن وجدت)
  requires?: string[];  // الصلاحيات المطلوبة
}
```

### PermissionCategory

```typescript
interface PermissionCategory {
  id: string;           // معرف فريد (مثل: users)
  name: string;         // الاسم بالإنجليزية
  nameAr: string;       // الاسم بالعربية
  icon: string;         // الأيقونة
  permissions: string[]; // قائمة معرفات الصلاحيات
}
```

### Role

```typescript
interface Role {
  id: string;           // معرف فريد
  name: string;         // اسم الدور
  description: string;  // الوصف
  permissions: string[]; // قائمة معرفات الصلاحيات
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 8. التنفيذ

### React (الحالي)

```typescript
// Context للصلاحيات
const PermissionContext = createContext<PermissionContextType>(null);

// دالة التحقق
function hasPermission(permission: string): boolean {
  if (currentUser.permissions.includes("__all__")) return true;
  return currentUser.permissions.includes(permission);
}

// مكون الإخفاء
function PermissionGate({ permission, children }) {
  if (!hasPermission(permission)) return null;
  return children;
}
```

### Django (المستقبل)

```python
# Model
class Permission(models.Model):
    id = models.CharField(primary_key=True)
    name = models.CharField(max_length=100)
    name_ar = models.CharField(max_length=100)
    category = models.CharField(max_length=50)
    parent = models.ForeignKey('self', null=True)

# Middleware
def has_permission(user, permission):
    if "__all__" in user.role.permissions:
        return True
    return permission in user.role.permissions

# Template Tag
@register.simple_tag
def permission_gate(user, permission):
    return has_permission(user, permission)
```
